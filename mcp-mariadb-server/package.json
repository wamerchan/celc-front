{
  "name": "mcp-mariadb-server",
  "version": "1.0.0",
  "description": "MCP server that connects to MariaDB database",
  "type": "module",
  "bin": {
    "mcp-mariadb-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "populate": "node ./scripts/populate_mock.js"
  },
  "files": ["build"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "mysql2": "^3.6.0",
    "zod": "^3.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}